package com.example.compasos_tv.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta base del tema Material generado por defecto por Android Studio.
// Se usa únicamente en CompaSOS_TVTheme (Theme.kt); las pantallas de la app
// (Screan/) definen sus propios colores oscuros específicos por archivo.

/** Color primario en modo oscuro. */
val Purple80 = Color(0xFFD0BCFF)
/** Color secundario en modo oscuro. */
val PurpleGrey80 = Color(0xFFCCC2DC)
/** Color terciario en modo oscuro. */
val Pink80 = Color(0xFFEFB8C8)

/** Color primario en modo claro. */
val Purple40 = Color(0xFF6650a4)
/** Color secundario en modo claro. */
val PurpleGrey40 = Color(0xFF625b71)
/** Color terciario en modo claro. */
val Pink40 = Color(0xFF7D5260)
